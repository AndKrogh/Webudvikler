import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'

const array = ['hej']

describe('addToUserList ', () => {
    it('test if user added correctly', () => {
    const array = ['hej']

    expect(array.toContain('hej'))
    })
  })


